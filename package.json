{
  "name": "calculator-cli",
  "version": "1.0.0",
  "description": "A simple Node.js CLI calculator with basic arithmetic operations",
  "main": "src/calculator.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.5.0"
  }
}
